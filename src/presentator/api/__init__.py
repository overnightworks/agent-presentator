"""Owns the outside edge: FastAPI routes and the WebSocket wire schemas.

Imports `presentator.application` and `presentator.contracts`; a route names no
port type, because the host hands it use cases rather than capabilities.
"""
