"""Owns the presentation logic: run state machine, prompts, intent gate, prefetch.

Imports `presentator.ports` and `presentator.contracts`, so the logic stays
runnable without a process, a network, or a clock.
"""
