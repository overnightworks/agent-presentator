---
addons:
  - '..'
title: Addon fixture
---

# Addon fixture

Loaded only by `addon-package.test.ts`, to prove this package resolves as a
Slidev addon from its own `package.json`, without depending on the example
deck.
