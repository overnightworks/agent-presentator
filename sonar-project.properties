sonar.organization=overnightworks
sonar.projectKey=overnightworks_agent-presentator
sonar.sources=src,frontend
sonar.tests=tests,frontend/tests

# Sonar refuses to index a file as source and test at once, and neither the
# installed packages nor a built deck are this repository's code.
sonar.exclusions=frontend/tests/**,frontend/node_modules/**,examples/**/dist/**
sonar.python.version=3.12
sonar.sourceEncoding=UTF-8

# The scan must not report before the server has judged the analysis.
sonar.qualitygate.wait=true

# The `sonar` job downloads this from the `tests` job's artifact before scanning.
sonar.python.coverage.reportPaths=reports/coverage.xml
sonar.javascript.lcov.reportPaths=reports/frontend-coverage/lcov.info
